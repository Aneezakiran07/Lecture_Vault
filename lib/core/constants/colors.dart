import 'package:flutter/material.dart';

class AppColors {
  static const background     = Color(0xFFFFFFFF);
  static const headerCard     = Color.fromARGB(255, 3, 89, 85);
  static const bodyText       = Color(0xFF000000);
  static const selectedButton = Color(0xFF89B0AE);
  static const unselectedBorder = Colors.grey;
  static const white          = Color(0xFFFFFFFF);
}